{
  "rules": {
    "perfis": {
      ".read": "auth != null",
      "$perfilId": {
        ".write": "auth != null && ((!data.exists() && newData.exists() && newData.child('criadorUid').val() === auth.uid) || (data.exists() && data.child('criadorUid').val() === auth.uid && (!newData.exists() || newData.child('criadorUid').val() === data.child('criadorUid').val())))",
        ".validate": "!newData.exists() || (newData.hasChildren(['id', 'nome', 'administrativo', 'avatar', 'criadorUid']) && newData.child('id').val() === $perfilId && newData.child('nome').isString() && newData.child('nome').val().length >= 2 && newData.child('nome').val().length <= 24 && newData.child('administrativo').isBoolean() && newData.child('avatar').hasChildren(['pele', 'cabelo', 'corCabelo', 'roupa']))"
      }
    },
    "historico": {
      ".read": "auth != null",
      "$registroId": {
        ".write": "auth != null && !data.exists() && newData.exists() && newData.child('usuarioUid').val() === auth.uid",
        ".validate": "newData.hasChildren(['id', 'perfilId', 'nome', 'categorias', 'inicioEm', 'fimEm', 'duracaoMs', 'limpezaConfirmada', 'limpoPor', 'usuarioUid']) && newData.child('id').val() === $registroId && newData.child('nome').isString() && newData.child('inicioEm').isNumber() && newData.child('fimEm').isNumber() && newData.child('fimEm').val() >= newData.child('inicioEm').val() && newData.child('duracaoMs').isNumber() && newData.child('limpezaConfirmada').val() === true"
      }
    },
    "estadoCafeteira": {
      ".read": "auth != null",
      ".write": "auth != null && ((!data.exists() && newData.exists() && newData.child('usuarioUid').val() === auth.uid) || (data.exists() && data.child('usuarioUid').val() === auth.uid && !newData.exists()))",
      ".validate": "!newData.exists() || newData.hasChildren(['perfilId', 'nome', 'categorias', 'inicioEm', 'usuarioUid'])"
    },
    ".read": false,
    ".write": false
  }
}
